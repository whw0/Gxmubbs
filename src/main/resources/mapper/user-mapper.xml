<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.whirlwind.gxmubbs.dao.UserMapper">

    <sql id="insertFields">
        username, password, salt, email, type, status, activation_code, header_url, create_time
    </sql>

    <sql id="selectFields">
        id, username, password, salt, email, type, status, activation_code, header_url, create_time
    </sql>

    <select id="selectById" resultType="User">
        select <include refid="selectFields"></include>
        from user
        where id = #{id}
    </select>

    <select id="selectByName" resultType="User">
        select <include refid="selectFields"></include>
        from user
        where username = #{username}
    </select>

    <select id="selectByEmail" resultType="User">
        select <include refid="selectFields"></include>
        from user
        where email = #{email}
    </select>

    <insert id="insertUser" parameterType="User" keyProperty="id">
        insert into user (<include refid="insertFields"></include>)
        values(#{username}, #{password}, #{salt}, #{email}, #{type}, #{status}, #{activationCode}, #{headerUrl}, #{createTime})
    </insert>

    <update id="updateStatus">
        update user set status = #{status} where id = #{id}
    </update>

    <update id="updateHeader">
        update user set header_url = #{headerUrl} where id = #{id}
    </update>

    <update id="updatePassword">
        update user set password = #{password} where id = #{id}
    </update>

</mapper>
<!--<?xml version="1.0" encoding="UTF-8" ?>-->
<!--<!DOCTYPE mapper-->
<!--        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"-->
<!--        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">-->
<!--<mapper namespace="com.whirlwind.gxmubbs.dao.UserMapper">-->

<!--    &lt;!&ndash;  这样写比较麻烦-->
<!--       <select id="selectById" resultType="User">-->
<!--           select id,username,password,salt,email,type,status,activation_code,header_url,create_time-->
<!--           from user-->
<!--           where id=#{id}-->
<!--       </select>-->

<!--       <select id="selectByName" resultType="User">-->
<!--           select id,username,password,salt,email,type,status,activation_code,header_url,create_time-->
<!--           from user-->
<!--           where username=#{username}-->
<!--       </select>-->

<!--       <select id="selectByEmail" resultType="User">-->
<!--           select id,username,password,salt,email,type,status,activation_code,header_url,create_time-->
<!--           from user-->
<!--           where email=#{email}-->
<!--       </select>&ndash;&gt;-->

<!--&lt;!&ndash;    private int id;&ndash;&gt;-->
<!--&lt;!&ndash;    private String username;&ndash;&gt;-->
<!--&lt;!&ndash;    private String password;&ndash;&gt;-->
<!--&lt;!&ndash;    private String salt;&ndash;&gt;-->
<!--&lt;!&ndash;    private String email;&ndash;&gt;-->
<!--&lt;!&ndash;    private int type;&ndash;&gt;-->
<!--&lt;!&ndash;    private int status;&ndash;&gt;-->
<!--&lt;!&ndash;    private String activationCode;&ndash;&gt;-->
<!--&lt;!&ndash;    private String headerUrl;&ndash;&gt;-->
<!--&lt;!&ndash;    private Date createTime;&ndash;&gt;-->


<!--    <sql id="selectFields">-->
<!--        select id,username,password,salt,email,type,status,activation_code,header_url,create_time-->
<!--    </sql>-->

<!--    <sql id="insertFields">-->
<!--        select username,password,salt,email,type,status,activation_code,header_url,create_time-->
<!--    </sql>-->

<!--    <select id="selectById" resultType="User">-->
<!--        select <include refid="selectFields"></include>-->
<!--        from user-->
<!--        where id=#{id}-->
<!--    </select>-->

<!--    <select id="selectByName" resultType="User">-->
<!--        select <include refid="selectFields"></include>-->
<!--        from user-->
<!--        where username=#{username}-->
<!--    </select>-->

<!--    <select id="selectByEmail" resultType="User">-->
<!--    select <include refid="selectFields"></include>-->
<!--    from user-->
<!--    where email=#{email}-->
<!--    </select>-->

<!--&lt;!&ndash; 因为User是自定义的，User作为传入参数要指定类型,在插入语句中主键是自动生成的，所以要指定主键&ndash;&gt;-->
<!--    <insert id="insertUser" parameterType="User" keyProperty="id">-->
<!--        insert into user(<include refid="insertFields"></include>)-->
<!--        values(#{username},#{password},#{salt},#{email},#{type},#{status},#{activationCode},#{headerUrl},#{createTime})-->
<!--    </insert>-->

<!--    <update id="updateStatus">-->
<!--        update user set status=#{status} where id=#{id}-->
<!--    </update>-->

<!--    <update id="updateHeader">-->
<!--        update user set header_url=#{headerUrl} where id=#{id}-->
<!--    </update>-->

<!--    <update id="updatePassword">-->
<!--        update user set password=#{password} where id=#{id}-->
<!--    </update>-->

<!--</mapper>-->


